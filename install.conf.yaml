- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/bat/config: bat/config
    ~/.gitignore_global:
    ~/.p10k.zsh: zsh/p10k.zsh
    ~/.pythonrc:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
