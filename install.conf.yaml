- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.config/bat: bat
    ~/.config/btop: btop 
    ~/.config/nvim: nvim
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.p10k.zsh: zsh/p10k.zsh
    ~/.pythonrc:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
